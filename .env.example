# Copiar para .env em desenvolvimento local (com python-dotenv ou similar),
# ou definir estas variáveis directamente no dashboard da Vercel (Settings → Environment Variables)
# antes do deploy.

DJANGO_SECRET_KEY=troque-por-uma-chave-secreta-longa-e-aleatoria
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=cientificando.com,www.cientificando.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://cientificando.com,https://www.cientificando.com,https://*.vercel.app

# Base de dados — a Vercel não tem disco persistente, por isso o SQLite
# NÃO funciona em produção. Usar Postgres externo (ex.: Neon, Supabase).
DATABASE_URL=postgresql://neondb_owner:npg_z2afbMlCiT3u@ep-orange-mouse-b2bwqdvi-pooler.c-6.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Armazenamento de imagens (fotos da equipa, projectos, artigos) — a Vercel
# apaga o sistema de ficheiros a cada deploy, por isso é obrigatório usar
# armazenamento externo em produção. Criar conta gratuita em cloudinary.com
# e colar aqui o "API Environment variable" que o Cloudinary fornece.
CLOUDINARY_URL=CLOUDINARY_URL=cloudinary://<your_api_key>:<your_api_secret>@wazawcmp

# Email transaccional (formulário de contacto)
DJANGO_EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=diano.budimboja@gmail.com
EMAIL_HOST_PASSWORD=nkhncivdwpdjcljf
EMAIL_USE_TLS=True

DEFAULT_FROM_EMAIL=diano.budimboja@gmail.com
CONTACT_NOTIFICATION_EMAIL=diano.budimboja@gmail.com
